<%- include("_header") -%>

<!-- Animated Header -->
<header class="text-center text-white py-5">
  <div class="container">
    <h1 class="animate__animated animate__slideInDown">Welcome to Awesome Automation Sales</h1>
    <p class="lead animate__animated animate__fadeInUp animate__delay-1s">
      Streamline your customer management with ease.
    </p>
    <br>
    <br>
    <!-- Robot Head -->
    <div id="robot" class="robot mx-auto">
      <!-- Antennas -->
      <div class="antenna left-antenna"></div>
      <div class="antenna right-antenna"></div>

      <!-- Robot Head -->
      <div class="robot-head">
        <!-- Eyes -->
        <div class="eye" id="left-eye"></div>
        <div class="eye" id="right-eye"></div>
        <!-- Mouth -->
        <div class="mouth"></div>
      </div>
    </div>
  </div>
</header>
<!-- Cards Section -->
<section class="container my-5">
  <div class="row text-center">
    <!-- Cards remain unchanged -->
    <div class="col-md-3">
      <div class="card shadow border-0 animate__animated animate__fadeIn animate__delay-0s">
        <div class="card-body">
          <h5 class="card-title">Manage Customers</h5>
          <p class="card-text">View, edit, or delete customer records</p>
          <a href="/managecust" class="btn btn-primary">Go</a>
        </div>
      </div>
    </div>
    <div class="col-md-3">
      <div class="card shadow border-0 animate__animated animate__fadeIn animate__delay-1s">
        <div class="card-body">
          <h5 class="card-title">Create Customer</h5>
          <p class="card-text">Add new customers to your database</p>
          <a href="/create" class="btn btn-success">Create</a>
        </div>
      </div>
    </div>
    <div class="col-md-3">
      <div class="card shadow border-0 animate__animated animate__fadeIn animate__delay-2s">
        <div class="card-body">
          <h5 class="card-title">Import Data</h5>
          <p class="card-text">Upload bulk customer data with ease</p>
          <a href="/import" class="btn btn-warning">Import</a>
        </div>
      </div>
    </div>
    <div class="col-md-3">
      <div class="card shadow border-0 animate__animated animate__fadeIn animate__delay-3s">
        <div class="card-body">
          <h5 class="card-title">Export Data</h5>
          <p class="card-text">Download customer data for reports</p>
          <a href="/export" class="btn btn-info">Export</a>
        </div>
      </div>
    </div>
  </div>
</section>

<%- include("_footer") -%>

<!-- Bootstrap Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<!-- Robot Eye Tracking Script -->
<script>
  const leftEye = document.getElementById("left-eye");
  const rightEye = document.getElementById("right-eye");

  document.addEventListener("mousemove", (e) => {
    const robot = document.querySelector(".robot");
    const rect = robot.getBoundingClientRect();
    const centerX = rect.left + rect.width / 2;
    const centerY = rect.top + rect.height / 2;

    // Calculate angle and distance for the eyes
    const angle = Math.atan2(e.clientY - centerY, e.clientX - centerX);
    const distance = Math.min(10, Math.hypot(e.clientX - centerX, e.clientY - centerY) / 15);

    const offsetX = Math.cos(angle) * distance;
    const offsetY = Math.sin(angle) * distance;

    leftEye.style.transform = `translate(${offsetX}px, ${offsetY}px)`;
    rightEye.style.transform = `translate(${offsetX}px, ${offsetY}px)`;
  });
</script>

<!-- Robot Styles -->
<style>
  /* Robot Container */
  .robot {
    position: relative;
    width: 150px;
    height: 150px;
    margin: 20px auto;
  }

  /* Antennas */
  .antenna {
    position: absolute;
    width: 10px;
    height: 30px;
    background-color: #e90000;
    border-radius: 5px;
  }
  
  .left-antenna {
    top: -30px;
    left: 30px;
  }

  .right-antenna {
    top: -30px;
    right: 30px;
  }

  /* Robot Head */
  .robot-head {
    position: relative;
    width: 100%;
    height: 100%;
    background-color: #9aa4af; /* Robot Blue */
    border-radius: 20px;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
  }

  /* Robot Eyes */
  .eye {
    position: absolute;
    width: 30px;
    height: 30px;
    background-color: rgb(29, 29, 29);
    border-radius: 50%;
    top: 40%;
    transform: translateY(-50%);
    box-shadow: inset 0 4px 6px rgba(42, 170, 255, 0.4);
  }

  #left-eye {
    left: 30px;
  }

  #right-eye {
    right: 30px;
  }

  .eye::before {
    content: "";
    position: absolute;
    width: 12px;
    height: 12px;
    background-color: rgb(15, 167, 255);
    border-radius: 50%;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
  }

  /* Robot Mouth */
  .mouth {
    position: absolute;
    bottom: 20px;
    left: 50%;
    width: 60px;
    height: 10px;
    background-color: black;
    transform: translateX(-50%);
    border-radius: 5px;
  }
</style>

  <!-- Light and Dark Mode Functionality (For Fun) -->
  <script>
    const themeToggle = document.getElementById('theme-toggle');
    const body = document.body;

    if (localStorage.getItem('theme') === 'dark') {
      body.classList.add('dark-mode');
      themeToggle.textContent = '☀️ Light Mode';
    }

    themeToggle.addEventListener('click', () => {
      body.classList.toggle('dark-mode');
      const isDarkMode = body.classList.contains('dark-mode');
      themeToggle.textContent = isDarkMode ? '☀️ Light Mode' : '🌙 Dark Mode';
      localStorage.setItem('theme', isDarkMode ? 'dark' : 'light');
    });
  </script>